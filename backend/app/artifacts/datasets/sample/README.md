# Sample Dataset

Put the real demo/sample inference dataset here when it is ready.

Recommended format:

```text
customers_sample.parquet
```

The sample should contain the exact fields required by the TabNet and TFT preprocessing/inference pipeline. Keep it small enough for demo usage, for example 1000-5000 customers.
